## 基本配置信息
# JDBC URL，根据不同的数据库，使用相应的JDBC连接字符串
url = jdbc:mysql://127.0.0.1:3306/jeewx
# 用户名，此处也可以使用 user 代替
username = root
# 密码，此处也可以使用 pass 代替
password = root
# JDBC驱动名，可选（Hutool会自动识别）
driver = com.mysql.jdbc.Driver
